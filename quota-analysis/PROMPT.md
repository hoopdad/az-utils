the capacity-planning folder has a bunch of scripts. make a new folder, quota analysis, that contains a script to take as input a CSV with subscription name (optional), subscription ID (optional), Azure REgion, SKU (optional), SKU Family (optional), quantity of cores (optional) or quantity of VMs (optional). One of SKU or SKU Family is required. One of cores or VMs is optional. Look up subscription name by subscription ID if missing using az cli commands. Look up subscription ID by subscription name if missing using az cli commands. If number of VMs is there but not number of cores, look up the number of cores per VM to calculate number of cores. If SKU is provided but not SKU Family, look up the SKU Family for the SKU. Add all these additional columns for every run to the output so it is uniform. Output into a designated output folder. Add the output folder to .gitignore if in this folder hierarchy. With this information above, use AZ CLI to find current quota consumption and total for that Subscription ID, Region, and SKU Family. Calculate remaining quota. Add a formula that calculates the current consumption + additional cores. Add a column that shows "additional quota needed" with either a zero if none is needed or the additional amount needed. Add a second sheet with all data for all VM Quota in each Subscription ID, Region, and SKU Family. Include consumption and total for each row. Please ask any clarifying questions then go!

Group the output by Subscription Name, Region, and SKU Family. There will often be duplicates in the input.  az login has been completed - iteratively run, debug, fix, and get me a great product!